{% extends 'base.html.twig' %}

{% block title %}{{ produit.id ? 'Edit' : 'New' }} Produit{% endblock %}

{% block body %}
    <h1>{{ produit.id ? 'Edit' : 'New' }} Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': produit.id ? 'Update' : 'Create'}) }}

    <a href="{{ path('app_produit_index') }}">back to list</a>

    {% if produit.id %}
        {{ include('produit/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
